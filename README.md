# Weather CLI

Simple Node.js CLI weather app using Open-Meteo (no API key required).

Usage

```bash
node index.js London
```

Files

- `index.js` — CLI entrypoint
- `api.js` — Geocoding and weather API calls (uses built-in `https`)
